# foreign-node

A tiny brownfield Node project used as a fixture for the reverse-engineer
Phase 0 ("detect & scope") helper. It deliberately has NO
`docs/_architect_state.json`, so `bin/re-detect` must classify it as a foreign
project to reverse-engineer.
